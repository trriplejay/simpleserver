language: java

jdk:
   - oraclejdk8

build:
  ci:
    - echo $JAVA_HOME
    - echo $PATH
    - java -version
    - mvn --version
    - gradle --version
    - source shipctl jdk set oraclejdk8
    - echo $JAVA_HOME
    - echo $PATH
    - java -version
    - mvn --version
    - gradle --version
    - source shipctl jdk set oraclejdk11
    - echo $JAVA_HOME
    - echo $PATH
    - java -version
    - mvn --version
    - gradle --version
    #- mvn clean test

notifications:
   email: false
