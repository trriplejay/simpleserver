# Language setting
language: node_js

# Version number
node_js:
  - 6.11.0

build:

  ci:
   - echo "ci"
  post_ci:
    - echo "post_ci section"
    
  on_success:
    - echo "huzzah!"

  push:
    # - docker push gcr.io/sample-gke/simpleserver:$environment.$BUILD_NUMBER

integrations:

  notifications:
       
    - integrationName: "shippable via trriplejay"
      type: slack
      recipients:
        - "@john"
        - "#slack-test"
      on_success: always
      on_failure: never

    - integrationName: "shippable via trriplejay"
      type: slack
      recipients:
        - "@john"
        - "#test-channel"
      on_success: never
      on_failure: always
